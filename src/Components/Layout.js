import styled from 'styled-components'
import { colors } from 'utilities'

const Layout = styled.div`
  min-height: 100vh;
  background: linear-gradient(
      to bottom,
      rgba(3, 25, 38, 1) 0%,
      rgba(3, 25, 38, 1) 9%,
      rgba(40, 82, 92, 1) 59%,
      rgba(70, 129, 137, 1) 100%
    )
    no-repeat;
`

export default Layout
